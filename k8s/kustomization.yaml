apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - network-policy.yaml

# CI rewrites this tag on every release.
images:
  - name: ghcr.io/loaspra/claude-code-openai-wrapper
    newTag: sha-74951748
