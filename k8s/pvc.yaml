apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-provider-state
  namespace: letta-agent
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
