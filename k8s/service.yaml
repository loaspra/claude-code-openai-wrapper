apiVersion: v1
kind: Service
metadata:
  name: claude-provider
  namespace: letta-agent
spec:
  type: NodePort
  externalTrafficPolicy: Local
  selector:
    app: claude-provider
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30457
      protocol: TCP
